{
  "name": "mern-plates",
  "version": "0.1.0",
  "description": "Boilerplates for projects, each branch represents a different boilerplate, check README of each branch.",
  "author": "mernxl <mernxl@gmail.com>",
  "license": "NOLICENSE",
  "private": true,
  "scripts": {
    "build": "webpack --config webpack.config.js --progress --colors --env",
    "build:watch": "webpack --config webpack.config.js --progress --colors --watch --env",
    "build:release": "webpack --config webpack.config.js --progress --colors --env.release"
  },
  "dependencies": {
    "ts-optchain": "^0.1.5"
  },
  "devDependencies": {
    "ts-loader": "^5.3.3",
    "tslint": "^5.10.0",
    "tslint-config-prettier": "^1.14.0",
    "tslint-plugin-prettier": "^1.3.0",
    "typescript": "^2.9.1",
    "webpack": "^4.29.0",
    "webpack-cli": "^3.2.1"
  }
}
